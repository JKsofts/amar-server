# This is an example file. Copy this to a new file named .env and fill in your actual credentials.
# Do not commit the .env file to version control.

# Your OpenAI API Key
OPENAI_API_KEY="your_openai_api_key_here"

# Your Firebase Service Account Credentials.
# This MUST be a single-line JSON string.
# Go to your Firebase project settings -> Service accounts -> Generate new private key.
# Open the downloaded JSON file, copy its content, and paste it here between the quotes.
# You can use an online tool to convert the multi-line JSON to a single line, or manually replace newlines with \n.
FIREBASE_CREDENTIALS='{"type": "service_account", "project_id": "...", "private_key_id": "...", "private_key": "...", ...}'